# ============================================================
# ABCD Ltd — Environment configuration
# ============================================================
# Copy this file to .env for local development.
# On Coolify, set these as environment variables in the dashboard.
# ============================================================

# ---- Database (SQLite) ----
# Local development:
DATABASE_URL="file:/home/z/my-project/db/custom.db"
# Production (Docker/Coolify) — points to the mounted volume:
# DATABASE_URL="file:/app/db/custom.db"

# ---- Admin user (created on first run by seed script) ----
ADMIN_EMAIL="admin@abcd.com"
ADMIN_PASSWORD="abcd2025"
ADMIN_NAME="Administrateur ABCD"

# ---- Server ----
NODE_ENV="production"
PORT=3000
HOSTNAME=0.0.0.0
